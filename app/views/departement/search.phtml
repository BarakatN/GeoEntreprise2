<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["departement/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["departement/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Num Of Dept</th>
            <th>Libelle</th>
            <th>Etablissement Of Id Of Etab</th>
            <th>Contact Of Id Of Contact</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $departement): ?>
            <tr>
                <td><?php echo $departement->num_dept ?></td>
            <td><?php echo $departement->libelle ?></td>
            <td><?php echo $departement->etablissement_id_etab ?></td>
            <td><?php echo $departement->contact_id_contact ?></td>

                <td><?php echo $this->tag->linkTo(["departement/edit/" . $departement->num_dept, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["departement/delete/" . $departement->num_dept, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("departement/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("departement/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("departement/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("departement/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
